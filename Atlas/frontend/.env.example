# Base URL of the standalone Atlas backend.
# In dev, Vite proxies "/atlas" and "/health" to this origin.
# The frontend NEVER calls Zoom Agentic AI directly.
VITE_ATLAS_API_BASE=http://localhost:8090
